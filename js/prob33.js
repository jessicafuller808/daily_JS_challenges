/**
 * 8kyu - Convert a String to a Number
 * TODO: We need a function that can transform a string into a number. What ways of achieving this do you know?
 */

 const stringToNumber = function(str){
    return parseInt(str);
  }

  console.log(stringToNumber('12345'));